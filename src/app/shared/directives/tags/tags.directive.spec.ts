import { TagsDirective } from './tags.directive';

describe('TagsDirective', () => {
  it('should create an instance', () => {
    // const directive = new TagsDirective();
    // expect(directive).toBeTruthy();
  });
});
